export const metadata = {
  title: "Mi perfil",
  description: "Tu cuenta en SIMPLE: puntos acumulados, historial de pedidos y nivel de fidelidad.",
}

export default function PerfilLayout({ children }) {
  return children
}
